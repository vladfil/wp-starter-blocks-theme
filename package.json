{
  "name": "wp-starter-blocks-theme",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "start": "vite build --watch",
    "build": "vite build",
    "blocks:start": "wp-scripts start --webpack-src-dir=blocks/src/ --output-path=blocks/build/",
    "blocks:build": "wp-scripts build --webpack-src-dir=blocks/src/ --output-path=blocks/build/"
  },
  "devDependencies": {
    "@wordpress/scripts": "^27.1.0",
    "sass": "^1.70.0",
    "vite": "^5.0.12"
  }
}
